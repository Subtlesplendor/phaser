# Multi-scalar model

This public Milestone 1 model exercises two ordered real scalar fields, mixing,
tadpoles, and every supported scalar tensor rank. Run `zig build examples` to
parse, validate, fingerprint, and inspect it through the public experimental Zig
API. `inspection.txt` is the reviewed deterministic output. The conformance
manifest references this same source document.
